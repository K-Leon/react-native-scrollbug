/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import scrollbug from './scrollbug'
import {
  AppRegistry,
} from 'react-native';
AppRegistry.registerComponent('scrollbug', () => scrollbug);
